[project]
name = "fruteria-papus"
version = "0.1.0"
description = "Gestión de inventario de frutería"
dependencies = [
    "Flask>=3.0.0",
    "pymongo[srv]>=4.6.0",
    "Flask-Mail>=0.9.1",
    "blinker>=1.7.0",
]

[tool.uv]
# Configuración opcional específica de uv si se requiere en el futuro
